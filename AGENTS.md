# LaunchPad Project Instructions

LaunchPad 是一个 Manifest V3 Chrome 扩展，包含 popup／new tab 快捷入口、
arXiv 检索过滤、Scour 收藏与学术趋势，以及 Google 查询、URL 直达、语音和 Lens 入口。
本文件是共享维护规则；`CLAUDE.md` 导入它，避免重复维护旧功能和定位表。

## 开始工作

- 先读当前任务涉及的源文件、最近的测试和 Git 状态，保留无关或并发改动。
- 简单文字／规则修改只需核对事实、路径和差异；功能修改先明确行为、验收和影响面。
- 在已有授权范围内完成工作；只有缺失决策会改变实际范围时才询问，并先完成不依赖该决策的部分。
- 下表是导航入口。用符号名查找当前代码，避免把旧行号或本文件的概述当作运行证据。

## 核心模块

| 功能 | 文件与符号 |
|---|---|
| 扩展入口与权限 | `manifest.json`；必需权限和 `optional_host_permissions` 分开核对 |
| 跨域请求代理 | `background.js` 的 runtime message handler；`core.js` 的 URL／请求校验 |
| Google 查询与 URL 直达 | `core.js`：`resolveGoogleSearchTarget()`；`newtab/newtab.js`：`navigateFromGoogleSearch()` |
| 持久化 | `core.js`：`STORAGE_KEYS`、`saveSettings()`、`createSettingsWriter()`；`newtab/newtab.js`：`readStoredSettings()`、`saveSettingsFields()` |
| 默认站点与共享显示辅助 | `shared.js`：`defaultSites`、`escapeHtml()`、`safeColor()`；popup 与 new tab 复用 |
| 快捷方式渲染与拖拽 | `popup.js`、`newtab/newtab.js`：`render()`、`reorder()`、拖拽事件 |
| arXiv 数据与缓存 | `newtab/newtab.js`：`loadArxivFeed()`、`readMatchingArxivCache()` |
| arXiv 过滤 | `filter-query.js`：`parseFilterQuery()`、`matchesFilterAst()`；`newtab/newtab.js`：过滤反馈与渲染 |
| Scour 数据与预览 | `newtab/newtab.js`：`loadScourFeed()`、`fetchScourItems()`、`parseScourHtml()`、`ensurePreviewPermission()` |
| 学术趋势 | `newtab/newtab.js`：`loadAcademicTrend()`；`academic-trend.js` |
| 界面与主题 | `popup.html`、`styles.css`、`newtab/index.html`、`newtab/newtab.css` |

## 维护约定

- 沿用 ES6+、camelCase 和现有 DOM／事件组织方式；保持最小连贯改动。
- 网络与存储失败需要可见状态或诊断，不能被当成空数据或成功保存。
- 以代码里的 `STORAGE_KEYS` 为存储键名的唯一清单；修改时检查既有用户数据和 local／sync 兼容性。
- 复用现有主题变量和共享辅助函数，不复制第二份默认站点或样式配置。
- Google 路由改 `resolveGoogleSearchTarget()`；表单、语音、Lens 入口在 new tab HTML／CSS 中维护。
- 新 arXiv 分类或分组应同步实际控件与 `defaultArxivGroups`；不要恢复已移除的多搜索引擎控件。
- 修改默认快捷方式时核对 `shared.js` 及两种界面的消费方式。

## 当前实现边界

- arXiv 已按分类匹配本地缓存，先显示缓存再刷新；缓存、部分来源失败与新结果必须保持可区分。
- `manifest.json` 的必需 host permissions 是明确来源列表；`https://*/*` 在可选权限中。
  外部预览应沿用当前按来源请求权限的路径，不扩大为静默必需权限。
- Scour 入口由 Settings 中的 `scourUrl` 配置，缓存必须绑定该来源；HTML 解析依赖站点结构，站点改版时检查实际解析器与 fixture。
- 过滤语法以 `FilterQuery.parseFilterQuery()` 为准；错误表达式保留最近有效结果，并显示未应用提示，不静默接受部分表达式。
- 这些说明定位当前实现；浏览器运行效果仍需对应验收，不能仅凭函数存在报告通过。

## 验证与完成

- 对行为修改先使用最近的测试，必要时增加能暴露缺陷的回归例；例如
  `node --test tests/core.test.js` 或 `node --test tests/background.integration.test.js`。
- 共享逻辑、权限／manifest、跨界面行为或发布前运行 `npm test`；需要覆盖率检查时使用
  `npm run test:coverage`。现有测试还包括 arXiv、学术趋势和 UI parity。
- 对受影响的浏览器路径检查：popup／new tab、快捷方式 CRUD 与拖拽、feed 加载与过滤、
  Scour 预览权限、Google 查询／URL／语音／Lens、Settings 保存、主题和重启后的数据保留。
- 视觉或浏览器 API 变化补充相应的真实界面检查；文字或规则修正不强制全部手工流程，
  也不新增只复述实现的测试。相关检查通过后，仅因新改动、失败或未解决疑点扩大／重复验证。
- 报告变更、实际验证、未运行检查及剩余风险；保留 partial／blocked。若当前任务要求浏览器验收，
  不得在该验收缺失时声称用户流程已验证。

## 安全边界

- 用户输入和远程内容进入 `innerHTML` 前必须转义或使用安全构造；优先 `textContent`。
- 校验外部 URL 与代理请求来源；外部链接使用 `rel="noopener"`。
- 敏感数据不存入 `chrome.storage.sync`；不泄露用户配置或扩展存储。

## 相关资源

- [Chrome Extension Manifest V3](https://developer.chrome.com/docs/extensions/mv3/)
- [arXiv RSS](https://arxiv.org/help/rss)
- [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/storage/)

<Codex-mem-context>
# Recent Activity

<!-- This section is auto-generated by Codex-mem. Edit content outside the tags. -->

*No recent activity*
</Codex-mem-context>
