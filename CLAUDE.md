# CLAUDE.md - LaunchPad Chrome Extension

> AI 辅助开发上下文文件，帮助 Claude 理解项目结构和维护规范。

## 项目概述

**LaunchPad** 是一个 Chrome 浏览器扩展 (Manifest V3)，提供：
- 自定义快捷启动面板 (popup + new tab)
- arXiv RSS 订阅与高级过滤
- Scour.ing 个人收藏聚合
- 多搜索引擎切换 (Google/Gemini/Perplexity/Claude/ChatGPT)

## 目录结构

```
LaunchPad/
├── manifest.json          # 扩展配置 (Manifest V3)
├── background.js          # Service Worker (跨域 fetch 代理)
├── popup.html/js          # 工具栏弹窗 UI
├── styles.css             # 共享样式 (popup)
├── newtab/
│   ├── index.html         # 新标签页 HTML
│   ├── newtab.js          # 新标签页逻辑 (约 1600 行)
│   └── newtab.css         # 新标签页样式
├── icons/                 # 扩展图标 (16/48/128px)
├── reference/             # 参考资料
└── .alma-snapshots/       # 版本快照 (忽略)
```

## 核心模块定位

| 功能 | 文件 | 行号范围 | 说明 |
|------|------|----------|------|
| 搜索引擎配置 | `newtab.js` | 75-106 | `SEARCH_ENGINES` 数组 |
| 数据持久化 | `newtab.js` | 108-120, 382-406 | 三层存储策略 |
| 快捷方式渲染 | `newtab.js` | 324-367 | `render()` 函数 |
| arXiv 加载 | `newtab.js` | 563-610 | `loadArxivFeed()` |
| arXiv 过滤语法 | `newtab.js` | 612-756 | tokenizer + parser |
| Scour 加载 | `newtab.js` | ~945-980 | HTML 解析主路径 + RSS 尝试 |
| 拖拽排序 | `newtab.js` | 1122-1194 | 通用拖拽处理 |
| 默认站点 | `popup.js` | 16-35 | `defaultSites` |
| 跨域请求 | `background.js` | 7-19 | message handler |

## 开发规范

### 代码风格
- 使用 ES6+ 语法 (const/let, arrow functions, async/await)
- 函数命名: camelCase，描述性动词开头 (renderXxx, loadXxx, handleXxx)
- DOM 元素引用在文件顶部统一声明
- 错误处理: 网络请求必须 try-catch

### 存储键名
```javascript
const STORAGE_KEYS = [
  'sites',              // 快捷方式列表
  'appLinks',           // Apps 菜单
  'accountLinks',       // Account 菜单
  'avatarUrl',          // 用户头像
  'arxivCategory',      // (legacy) 单分类
  'arxivCategories',    // 多分类数组
  'arxivRefreshMinutes',// 自动刷新间隔
  'arxivCustomGroups',  // 自定义分组
  'arxivFilter',        // 过滤关键词
  'arxivFilterMode',    // 'any' | 'all'
  'searchEngine'        // 当前搜索引擎 ID
];
```

### CSS 变量 (系统主题适配)
```css
:root {
  --canvas: Canvas;           /* 背景色 */
  --canvas-text: CanvasText;  /* 前景色 */
  --muted-text: GrayText;     /* 次要文字 */
  --surface: ...;             /* 卡片背景 */
  --accent: Highlight;        /* 强调色 */
}
```

## 常见维护任务

### 添加新搜索引擎
1. 在 `newtab.js` 的 `SEARCH_ENGINES` 数组添加配置
2. 在 `newtab/index.html` 的 `#searchEngineSelect` 添加 `<option>`

### 添加新 arXiv 分类
1. 在 `newtab/index.html` 的 `#arxivCategorySelect` 和 `.settings-chips` 添加
2. 可选：在 `defaultArxivGroups` 添加预设分组

### 修改默认快捷方式
编辑 `popup.js` 和 `newtab.js` 中的 `defaultSites` 数组

### 调试跨域请求
检查 `background.js` 的 message handler 和 `manifest.json` 的 `host_permissions`

## 测试检查清单

- [ ] popup 弹窗正常打开
- [ ] 新标签页替换生效
- [ ] 快捷方式增删改、拖拽排序
- [ ] arXiv feed 加载、过滤语法
- [ ] Scour feed 加载、预览展开
- [ ] 搜索引擎切换
- [ ] Settings 面板保存
- [ ] 深色/浅色主题切换
- [ ] 数据在浏览器重启后保留

## 已知限制

1. **宽泛的 host_permissions**: `https://*/*` 用于 favicon 和预览，可能触发安全审核
2. **arXiv 无本地缓存**: 每次新标签页都重新请求
3. **Scour URL 硬编码**: `https://scour.ing/@dtjgp`
4. **过滤语法错误静默回退**: 无用户提示
5. **Scour 解析依赖 `[data-item="post"]` 选择器**: 如 scour.ing 改版则需更新 `parseScourHtml()`

## 安全注意事项

- 不要在 `innerHTML` 中直接插入用户输入，使用 `textContent` 或转义
- 外部链接添加 `rel="noopener"`
- 敏感数据不要存入 `chrome.storage.sync` (会同步到云端)

## 相关资源

- [Chrome Extension Manifest V3 文档](https://developer.chrome.com/docs/extensions/mv3/)
- [arXiv RSS API](https://arxiv.org/help/rss)
- [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/storage/)


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>